Ejercicio1
	MainActivity.java	- Activity de la pantalla principal.
	DrawView.java		- View para dibujar un cuadrado en el lugar pulsado.

Ejercicio2
	MainActivity.java		- Activity de la pantalla principal.
	ButtonCircleActivity.java	- Captura el evento para dibujar dos círculos vacíos.
	ButtonFilledCircleActivity.java	- Captura el evento para dibujar dos círculos rellenos. 
	ButtonFilledRectActivity.java	- Captura el evento para dibujar dos rectángulos vacíos.
	ButtonRectActivity.java		- Captura el evento para dibujar dos rectángulos rellenos.
	ButtonSurpriseActivity.java	- Captura el evento para dibujar un triángulo relleno.
	CircleView.java			- View para dibujar dos círculos vacíos.
	FilledCircleView.java		- View para dibujar dos círculos rellenos. 
	FilledRectView.java		- View para dibujar dos rectángulos vacíos.
	RectView.java			- View para dibujar dos rectángulos rellenos.
	SurpriseView.java		- View para dibujar un triángulo relleno.
	
Ejercicio3
	MainActivity.java		- Activity de la pantalla principal.
	CameraIntent.java		- Activity que abre la camara y permite capturar imágenes
	GalleryView.java		- Activity que muestra todas las imágenes en una galería. 
	InterestingPointActivity.java	- Activity que muestra una pantalla con 4 botones y captura sus eventos de click.
	InterestingPointEnum.java	- Enum que representa los 5 tipos de puntos de interés existentes.	
	Map.java			- MapActivity que muestra el mapa google con la localización que le indiquemos
	ImageAdapter.java		- BaseAdapter que deposita la colección de imágenes en la gallería.	
	
Ejercicio4
	MainActivity.java		- Activity de la pantalla principal.
	CameraIntent.java		- Activity que abre la camara y permite capturar imágenes
	GalleryView.java		- Activity que muestra todas las imágenes en una galería. 
	InterestingPointActivity.java	- Activity que muestra una pantalla con 4 botones y captura sus eventos de click.
	InterestingPointEnum.java	- Enum que representa los 5 tipos de puntos de interés existentes.	
	Map.java			- MapActivity que muestra el mapa google con la localización que le indiquemos	ButtonCircleActivity.java	- Captura el evento para dibujar dos círculos vacíos.
	ImageAdapter.java		- BaseAdapter que deposita la colección de imágenes en la gallería.
	PhotoView.java			- Activity con los 5 botonoes para dibujar figuras geométricas sobre la imagen.
	ButtonFilledCircleActivity.java	- Captura el evento para dibujar dos círculos rellenos. 
	ButtonFilledRectActivity.java	- Captura el evento para dibujar dos rectángulos vacíos.
	ButtonRectActivity.java		- Captura el evento para dibujar dos rectángulos rellenos.
	ButtonSurpriseActivity.java	- Captura el evento para dibujar un triángulo relleno.
	CircleView.java			- View para dibujar dos círculos vacíos.
	FilledCircleView.java		- View para dibujar dos círculos rellenos. 
	FilledRectView.java		- View para dibujar dos rectángulos vacíos.
	RectView.java			- View para dibujar dos rectángulos rellenos.
	SurpriseView.java		- View para dibujar un triángulo relleno.